[tool.black]
line-length = 88
target-version = ["py310"]
include = '\.pyi?$'

[tool.isort]
profile = "black"

[tool.ruff]
line-length = 88
select = ["E", "F", "W", "C90"]

[tool.mypy]
python_version = 3.10
check_untyped_defs = true
ignore_missing_imports = true
